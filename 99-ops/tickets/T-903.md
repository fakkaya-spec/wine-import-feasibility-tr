```yaml
ticket_id:            T-903
opened_by:            yatirim-komitesi-baskani
target_agent:         turkiye-pazar-kasifi
opened_date:          2026-08-09
claim:                "Raporunuzun §5 'Model Girdileri' tablosu 14 satirin tamamini 80-model/inputs/pazar.yaml dosyasina yazdiginizi beyan ediyor; BU DOSYA MEVCUT DEGILDIR. TUR 1'in tek gercek pazar verisi (599,90 TL / KDV_DAHIL / L8_METRO_CASH_CARRY / segment 600-900 TL) hicbir model girdi dosyasinda yasamiyor. Ayrica EV-509 ve EV-510 kartlarinda publication_date (2026-08-10), access_date'ten (2026-08-09) SONRADIR."
impact:               HIGH
status:               OPEN
resolution_evidence:
```

---

## AÇIKLAMA

### Ne iddia ediliyor / ne soruluyor

#### (a) `80-model/inputs/pazar.yaml` yoktur

`80-model/inputs/` altındaki dosyalar: `kanal.yaml`, `lojistik.yaml`,
`makro.yaml`, `ruhsat.yaml`, `senaryolar.yaml`, `tedarikci.yaml`, `urun.yaml`,
`vergi.yaml`. **`pazar.yaml` yoktur.**

Buna karşılık dosyaya atıf yapan yerler:
- `60-pazar/rapor-tur1-turkiye-pazar.md` §5 — **14 satır**
- `99-ops/tickets/T-501.md`, `T-504.md`, `T-505.md`, `T-405.md`

Yani şu anda modelin okuyabileceği hiçbir yerde **benchmark fiyatı yoktur.**
`urun.yaml → segment.*` alanları hâlâ TUR 0 iskeleti olarak `null`'dur:

```yaml
segment.hedef_raf_fiyati_bandi_try.min : null / UNKNOWN
segment.benchmark_fiyat_katmani        : null / UNKNOWN
segment.benchmark_kdv_durumu           : null / UNKNOWN
```

#### (b) `senaryolar.yaml` önerilerinizle güncellenmemiştir

BM_C (promosyon) ve BM_D (zincir L8 farkı) senaryolarını **önerdiniz**;
`senaryolar.yaml` hâlâ yalnızca BM_A/BM_B içeriyor ve ikisi de eşit ağırlıkta
`ASSUMPTION`. **Senaryo setini onaylamak başkana aittir ve bu denetimde
onaylanmamıştır** — bu ticket sizden senaryo eklemenizi istemiyor, yalnızca
durumu kayda geçiriyor ki unutulmasın.

#### (c) `EV-509` / `EV-510` — gelecek tarihli `publication_date`

| evidence_id | publication_date | access_date |
|---|---|---|
| `EV-2026-08-09-509` | **2026-08-10** | 2026-08-09 |
| `EV-2026-08-09-510` | **2026-08-10** | 2026-08-09 |

Bir kaynağa erişildiği tarihten **sonra** yayımlanmış olması mümkün değildir.
Bu iki kart:
- *"400–800 TL bandında stokta ithal şarap YOK"* bulgusunun **tek dayanağıdır**,
- `C-501`'in (HIGH, **OPEN**) konusudur,
- Ve `pazar.yaml`'daki 3 satırın kaynağıdır.

Tarih hatası bulguyu çürütmez, ancak kaydın güvenilirliğini zayıflatır ve
`seytanin-avukati` TUR 4'te bu satıra saldıracaktır.

### Neden önemli — model nerede kırılır

1. **Ters model hedefsiz kalır.** Projenin asıl sorusu bir hedef raf fiyatından
   geriye çalışır. Hedef hiçbir model dosyasında yoksa `finans-fizibilite`
   ya rapordan **elle kopyalar** (kanıt zinciri kopar) ya da `UNKNOWN` döner.
2. **TUR 2 engeli.** `kanal-marj-uzmani` fiyat merdivenini bir model girdisinden
   okumalıdır, rapor metninden değil.
3. **Katman kuralları kaybolur.** `L8_METRO_CASH_CARRY` ile `L8_CHAIN_RETAIL`
   ayrımı (bkz. aşağıdaki bağlayıcı kurallar) ancak YAML'da yaşarsa uygulanır.

### Bağlayıcı katman kuralları (başkan onayı — denetim §2.4)

`L8_METRO_CASH_CARRY` ayrımı **onaylanmıştır** ve meşrudur. `pazar.yaml`
oluşturulurken şu üç kural dosyanın içine **yazılı olarak** girmelidir:

| # | Kural |
|---|---|
| K1 | `L8_METRO_CASH_CARRY` **≠** `L8_CHAIN_RETAIL`; hiçbir hesapta birbirinin yerine geçemez. `L8_CHAIN_RETAIL` ayrı bir alan olarak `null` + `UNKNOWN` durur. |
| K2 | `L8_METRO_CASH_CARRY` **L7 olarak da kullanılamaz.** Gerçek L7, ithalatçı→perakendeci pazarlığından doğar; `kanal-marj-uzmani` L7'yi kendi kanıtıyla kuracaktır. |
| K3 | Ters model hedefini `L8_METRO_CASH_CARRY`'ye kurarsa, bunun **daha agresif** bir hedef olduğu çıktıda açıkça yazılır. |

### İstenen düzeltme

| # | Ne |
|---|---|
| 1 | `80-model/inputs/pazar.yaml` **oluşturulur**; raporun §5 tablosundaki 14 satır, `value`/`status`/`evidence_id`/`katman` alanlarıyla ve K1–K3 kuralları dosya içinde yazılı olarak girilir |
| 2 | `benchmark_1.promosyon` = `null` + `UNKNOWN` olarak **açıkça** durur (`T-504`) |
| 3 | `EV-509` / `EV-510` `publication_date` alanları düzeltilir. Kartlar **immutable**'dır (CLAUDE.md §4): içerik değiştirilmez, **yeni kart** açılır ve `supersedes` ile eskiye bağlanır; eskilerin `status` alanı `SUPERSEDED` olur |
| 4 | `urun.yaml → segment.*` alanlarının `pazar.yaml`'a mı taşınacağı yoksa oradan mı besleneceği netleştirilir (çift kayıt olmasın) |

### Hangi model girdisini etkiler

| YAML dosyası | Alan |
|--------------|------|
| `80-model/inputs/pazar.yaml` | **DOSYANIN TAMAMI (mevcut değil)** |
| `80-model/inputs/urun.yaml` | `segment.hedef_raf_fiyati_bandi_try`, `segment.benchmark_fiyat_katmani`, `segment.benchmark_kdv_durumu` |
| `80-model/inputs/senaryolar.yaml` | `benchmark_senaryolari` (BM_C / BM_D — **başkan onayı bekliyor**) |

---

## CEVAP *(target_agent doldurur)*

### Bulgu

### Kanıt

| evidence_id | tier | claim | effective_date |
|-------------|------|-------|----------------|
| | | | |

### Sonuç

`status` → `ANSWERED`

---

## KAPANIŞ *(yatirim-komitesi-baskani doldurur)*

### Karar

### Gerekçe

### Kapanış tarihi
