# T-802

```yaml
ticket_id:            T-802
opened_by:            navlun-lojistik-uzmani
target_agent:         yatirim-komitesi-baskani
opened_date:          2026-08-10
claim:                "Projedeki TEK gercek navlun verisi (10 LCL kotasyon karti) 2026-08-16'da olur. 2026-08-17'den sonra calistirilan her model, lojistik girdisini kaynaksiz bir ESTIMATE olarak kullanmis olur."
impact:               HIGH
status:               OPEN
resolution_evidence:  -
```

---

## AÇIKLAMA

### Ne iddia ediliyor

```
access_date                    = 2026-08-10
ttl                            = 6d
kaynagin kendi gecerliligi     = 2026-08-16  (Flexport quote validity)
SON GECERLI GUN                = 2026-08-16
STALE                          = 2026-08-17
```

**İki bağımsız kontrol de aynı tarihi veriyor** (`veri-tazeligi.md`
§ÖZEL KONTROL gereği ayrı yapıldı):
`ttl` kontrolü → 2026-08-16 · kaynağın kendi geçerliliği → 2026-08-16.

Yani bu kartlar `ttl` dolduğunda **sadece eskimez, kaynak tarafından geri
çekilmiş olur.** İkinci kontrol birinciyi gevşetmez, **doğrular**.

Etkilenen 10 kart:
`EV-2026-08-10-301, -302, -303, -305, -306, -307, -308, -309, -310, -311`
(sayım düzeltmesi için bkz. `T-801`).

**Türev kartlar da düşer:** `EV-2026-08-10-329` (şişe başı lojistik maliyeti)
ve `EV-2026-08-10-330` (LCL/FCL kırılma noktası) girdilerini bu 10 karttan
alır. Kendi `ttl`'leri 14d olsa bile — **bir türev, kaynağından daha taze
olamaz.** 2026-08-17'de bunlar da STALE sayılmalıdır.

### Neden önemli — model nerede kırılır

Bu, "bir kanıt eskidi" meselesi değildir. **Projede FCL navlunu `UNKNOWN`'dır**
(`T-304` çekirdeği, `C-311` açık). Dolayısıyla LCL kotasyonları
**projedeki TEK gerçek, tarihli, kaynaklı navlun verisidir.**

| Tarih | Lojistik girdisinin durumu |
|---|---|
| ≤ 2026-08-16 | LCL bacağı `FACT`/`MEDIUM`; FCL bacağı `UNKNOWN` → girdi **kısmen kanıtlı** |
| ≥ 2026-08-17 | LCL bacağı `ESTIMATE`/`LOW`; FCL bacağı `UNKNOWN` → girdi **bütünüyle kanıtsız** |

**STALE bir kanıt otomatik olarak geçerli `FACT` gibi kullanılamaz**
(`veri-tazeligi.md` §KURALLAR). Bu yüzden 2026-08-17'den sonra çalıştırılan
bir ters model, lojistik maliyetini **CLAUDE.md §1.6'ya aykırı biçimde**
(evidence_id'si ölü bir sayı) kullanmış olur — ya da kendi çıktısını
`ESTIMATE / LOW` olarak damgalamak zorunda kalır.

### İstenen aksiyon

1. **Ters model 2026-08-16 24:00'a kadar çalıştırılsın.** Tazeliğin bedava
   olduğu son gün budur.
2. Bu tarih kaçırılırsa **iki seçenek vardır, üçüncüsü yoktur**:
   - (a) 10 kart **yeniden doğrulanır** — yeni kart açılır, `supersedes` ile
     eskiye bağlanır, eskinin `status`'ü `SUPERSEDED` olur;
   - (b) lojistik girdisi **`ESTIMATE / LOW`** olarak yeniden etiketlenir ve
     model çıktısının confidence'ı buna göre düşürülür.
3. `T-304` §4'te başkanın açtığını belirttiği **`T-913`** (yeniden doğrulama
   protokolü) ve **`OQ-912`** (TUR 3 çalıştırma tarihi) için
   `99-ops/tickets/` altında **dosya bulunmamaktadır**. Bu ticket onların
   yerini almaz; **yalnızca lojistik ayağının somut takvimini** verir.

### Hangi model girdisini etkiler

| YAML dosyası | Alan |
|--------------|------|
| `lojistik.yaml` | `tur2.lcl_navlun_rota_bazli.*` (tamamı) |
| `lojistik.yaml` | `tur2.sise_basi_lojistik_maliyeti.*`, `tur2.lcl_fcl_kirilma_tur2.*` |
| `lojistik.yaml` | `tur25_senaryolar.lcl_okyanus_usd_per_sise.*` |
| `lojistik.yaml` | `tur25_senaryolar.hacim_senaryolari_ispanya.*` (LCL satırları) |
| `lojistik.yaml` | `tur25_senaryolar.lcl_ust_sinir_capasi` (koşul (c) ile doğrudan bağlı) |
| `lojistik.yaml` | `tur25_senaryolar.ters_model_onerilen_girdi.*` |
| `senaryolar.yaml` | lojistik duyarlılık senaryoları |

---

## CEVAP *(target_agent doldurur)*

### Bulgu

### Kanıt

| evidence_id | tier | claim | effective_date |
|-------------|------|-------|----------------|
| | | | |

### Sonuç

---

## KAPANIŞ *(yatirim-komitesi-baskani doldurur)*

### Karar

### Gerekçe

### Kapanış tarihi
