# TICKET T-965

```yaml
ticket_id:            T-965
opened_by:            yatirim-komitesi-baskani
target_agent:         gumruk-vergi-uzmani
opened_date:          2026-08-10
claim:                "senaryolar.yaml -> tarih_senaryolari BAGLAYICI KURALI, bugun dogrulanmis OTV tutarinin (CURRENT_CONFIRMED 71,2692 TL/lt) UC HEDEF TARIHIN (2027-01-01 / 2027-04-01 / 2027-07-01) HICBIRI icin gecerli kullanilamayacagini soyler. AYNI KURAL FX ICIN YAZILMAMISTIR. TUR 3.25'te 2026-08 tarihli GOZLENEN spot kur alinmaktadir; bu kurun 2027 hedef tarihlerinde kullanilabilirligi DOGRULANMAMISTIR. T-858(b) ayni sinif hatanin BANDROL icin yapildigini zaten gostermistir."
impact:               HIGH
status:               OPEN
resolution_evidence:  -
```

---

## AÇIKLAMA

### Ne soruluyor

#### Soru 1 — "gelecek değer yazma" kuralı `fx`'e uygulanır mı?

Projede **ÖTV için titizlikle uygulanan** bir kural vardır:

> *"CURRENT_CONFIRMED (71,2692 TL/lt) bu üç tarihin HİÇBİRİ için geçerli
> tutar olarak kullanılamaz."*
> — `senaryolar.yaml` → `tarih_senaryolari.baglayici_kurallar`

`T-858`(b) bu kuralın **bandrol birim bedeli için atlandığını**
göstermiştir. **Aynı soru şimdi `fx` için sorulmaktadır:**

- 2026-08 tarihli gözlenen spot kur, model hedef tarihlerinde
  (2027-01 / 2027-04 / 2027-07) **kullanılabilir mi**?
- Kullanılamıyorsa, kur bir **projeksiyon noktası** olarak mı taşınmalıdır
  (ÖTV `λ` ekseni deseninde)?
- Kullanılabiliyorsa **gerekçesi nedir** — ve bu gerekçe ÖTV/bandrol için
  neden geçerli değildir? *(Tutarlılık bu ticket'ın çekirdeğidir: aynı
  belgede iki farklı tarih rejimi olamaz.)*

#### Soru 2 — **gümrük beyan kuru** ayrımı (bu turun gözlemi bunu KAPATMAZ)

`T-911`'de zaten sorulmuş olan ayrım burada tekrar kayda geçirilir:
gümrükte beyan edilecek kur **serbest piyasa kurundan farklı olabilir**
(kanunî kur / vergi kuru / hangi tarihin kuru).

**Yatırımcının bir kur vermesi veya ajanın spot kuru gözlemlemesi bu ayrımı
KAPATMAZ.** Modelde iki ayrı yerde iki ayrı kur gerekiyorsa, bu **açıkça**
yazılmalıdır:

| Kullanım yeri | Hangi kur | Kaynak kuralı |
|---|---|---|
| Gümrük kıymeti / matrah (`L2`→`L4`) | ? | ? |
| Ticari sözleşme / ödeme (`L0`/`L1`) | ? | ? |
| Raporlama / karar | ? | ? |

#### Soru 3 — kurun **hangi tarihi** bağlar?

`publication_date` / `effective_date` / `access_date` ayrımı
(`CLAUDE.md §4`) kur kanıt kartında nasıl doldurulmalıdır? Bir kur
gözleminin `ttl`'i nedir?

### Neden önemli — model nerede kırılır

`fx` bugün `null`'dur ve **üç turdur** kapanmamıştır. Gözlem alındığında
risk, sorunun **kapandığı sanılmasıdır.** Gözlenen bir kur:

- hedef tarihte geçerli olmayabilir *(Soru 1)*,
- gümrük matrahında kullanılan kur olmayabilir *(Soru 2)*,
- ve `ttl`'i tanımsız olabilir *(Soru 3)*.

Üçü de kapanmadan `fx` **`FACT` olarak modele giremez** — en fazla
tarihli bir **gözlem** olarak girer ve türevleri etiketlenir.

### Hangi model girdisini etkiler

| YAML dosyası | Alan |
|--------------|------|
| `80-model/inputs/makro.yaml` | `fx.*` |
| `80-model/inputs/senaryolar.yaml` | `duyarlilik_eksenleri[FX]`, `tarih_senaryolari` |
| `80-model/inputs/vergi.yaml` | matrah hesabında kullanılan kur tanımı |

### İlgili kayıtlar

`T-911` · `T-852` (**CRITICAL**) · `T-912` (**CRITICAL**) · `T-858`(b)
*(bandrol — aynı sınıf hata)* · `T-104` *(ÖTV λ ekseni)* · `T-964`

---

## CEVAP *(target_agent doldurur)*

### Bulgu

### Kanıt

| evidence_id | tier | claim | effective_date |
|-------------|------|-------|----------------|
| | | | |

### Sonuç

`status` → `ANSWERED`

---

## KAPANIŞ *(yatirim-komitesi-baskani doldurur)*

### Karar

### Gerekçe

### Kapanış tarihi
