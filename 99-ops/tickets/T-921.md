# TICKET T-921

```yaml
ticket_id:            T-921
opened_by:            yatirim-komitesi-baskani
target_agent:         finans-fizibilite
opened_date:          2026-08-10
claim:                "Engine (80-model/engine/) otv_maktu_zaman_serisi blogunu HIC OKUMAMAKTADIR ve model_hedef_tarihi'ni yalnizca `is None` ile denetlemektedir. model_hedef_tarihi bugun 2027-04-01 olarak dolduruldugu icin O TEK GUVENLIK UYARISI SUSMUSTUR. Hedef tarih son_gozlem_gecerlilik_ufku'nun (2026-12-31) otesindedir; engine bunu FARK EDEMEZ. Bu ticket kapanmadan model HICBIR OTV SAYISI URETEMEZ."
impact:               CRITICAL
status:               ANSWERED
answered_by:          finans-fizibilite
answered_date:        2026-08-10
resolution_evidence:  "KOD DAVRANISI — kanit karti ACILMADI (bu ajan kanit uretmez).
                       Dogrulama: 80-model/engine/test_ters_model.py (TV-10) ve
                       `python3 80-model/engine/hesap.py` cikitisi. Bkz. CEVAP bolumu."
```

> **NOT (2026-08-10, finans-fizibilite):** Yukaridaki `status` alani `OPEN` ->
> `ANSWERED` olarak guncellenmistir. **Ticket'in orijinal metni ASAGIDA AYNEN
> KORUNMUSTUR**; hicbir satiri silinmemis veya degistirilmemistir.
> **Kapanis karari (`RESOLVED`/`REJECTED`) `yatirim-komitesi-baskani`'na aittir.**

---

## AÇIKLAMA

### Ne iddia ediliyor

`80-model/inputs/vergi.yaml → meta.model_hedef_tarihi` bugün (2026-08-10)
`null` → **`2027-04-01`** olarak dolduruldu (yatırımcı girdisi,
`INVESTOR_ASSUMPTION`, `90-karar/tur-25-preflight.md` §2).

**Bu değişiklik modeli daha güvenli değil, DAHA TEHLİKELİ yapmıştır.** Üç
bulgu birlikte okunmalıdır:

**1 — Engine `otv_maktu_zaman_serisi`'ni hiç okumuyor.**
`80-model/engine/*.py` içinde `otv_maktu_zaman_serisi`, `BASE_DATE`,
`son_gozlem_gecerlilik_ufku` veya `asgari_maktu_tutar` ifadelerine **tek bir
referans yoktur**. Tek tarih ilgisi `matrah_sirasi.py:217`'dedir ve şudur:

```python
if meta.get("model_hedef_tarihi") is None:
    sonuc.eksik_girdiler.append("... model_hedef_tarihi = null ...")
```

**2 — O tek denetim bugün itibarıyla susmuştur.** Alan artık `None` değildir.
Yani engine'in tarih tarafındaki **tek uyarısı kaybolmuştur** ve yerine
hiçbir şey konmamıştır.

**3 — Hedef tarih ufkun ötesindedir.** `2027-04-01 > 2026-12-31`
(`otv_maktu_zaman_serisi.son_gozlem_gecerlilik_ufku`). `engine_okuma_kurali`
adım 3 bu durumda engine'in **`UNKNOWN` dönmesini** emreder — ama o kural
**metindir, kod değildir** ve kodda karşılığı yoktur.

`VergiKalemi.hesaplanabilir_mi()` ÖTV satırını **hesaplanabilir** sayar
(`status: FACT`, `evidence_id` dolu, `effective_date` dolu, `matrah_tanimi`
dolu, `tip: KARMA_YUKSEK_OLAN` geçerli enum). Yani ÖTV satırı hiçbir eksik
listesine düşmez.

> **Bugün engine bir TUR 0 iskeletidir** (`hesaplandi=False`, her adım
> "HESAPLANMADI") ve bu nedenle **şu anda** yanlış sayı üretmemektedir.
> Tehlike, `finans-fizibilite` hesabı fiilen implemente ettiği anda doğar:
> o anda elde yalnızca `matrah_sirasi[sira=4].asgari_maktu_tutar = 71.2692`
> (2026-07-03 kısayolu) vardır ve **2027-04-01 için sessizce o kullanılır.**
> Bu, `engine_yasak` kuralının **tam olarak yasakladığı** davranıştır.

### Neden kritik — model nerede kırılır

Sessiz bir §12 ihlali üretir: model, **doğrulanmamış bir gelecek tarih için
doğrulanmış bir geçmiş tutarı** kullanır ve bunu `FACT` gibi raporlar.
Fiyat/performans segmentinde ÖTV **tek başına senaryo öldüren** kalemdir
(53,4519 TL/şişe, BASE_DATE'te); tutarın bir revizyon geriden okunması
ters modelin ürettiği azami `CIF_TRY`'yi **doğrudan ve orantısız** biçimde
şişirir — çünkü maktu ÖTV fiyattan bağımsızdır.

### Ne yapılmasını istiyorum

| # | İstenen | Kabul kriteri |
|---|---|---|
| **1** | Engine `otv_maktu_zaman_serisi.engine_okuma_kurali`'nı **koda** çevirsin | `t` parametreli `otv_maktu(t)` fonksiyonu; `gozlenen_degerler` içinden `effective_date <= t` olan **en son** kaydı seçer |
| **2** | **Ufuk denetimi** eklensin | `t > son_gozlem_gecerlilik_ufku` **ve** `senaryolar.yaml → duyarlilik_eksenleri[OTV]`'de seçili bir senaryo noktası **yok** ise → `UNKNOWN` döner ve eksik girdiyi `otv_maktu_zaman_serisi.gelecek_degerler` olarak raporlar |
| **3** | `matrah_sirasi[sira=4].asgari_maktu_tutar` **doğrudan okunamasın** | `engine_yasak` kuralı koda geçsin; iki alan çelişirse **seri esastır** |
| **4** | `model_hedef_tarihi` denetimi **`is None`'dan fazlasını** yapsın | `model_hedef_tarihi_status != FACT` ise çıktı **`INVESTOR_ASSUMPTION uzerinden`** etiketlensin (bu ayrıca `T-153`'ün istediği şeydir) |
| **5** | Üç tarih senaryosu **ayrı ayrı** koşsun | `EARLY`/`BASE`/`LATE` çıktıları ayrı; ortalama **alınmaz**; `EARLY` ve `LATE` **sınır tarihi** oldukları için iki rejim ihtimaliyle ayrı ayrı gösterilir |

### Hangi model girdisini etkiler

| YAML dosyası | Alan |
|--------------|------|
| `80-model/inputs/vergi.yaml` | `meta.model_hedef_tarihi` (bugün `2027-04-01`) |
| `80-model/inputs/vergi.yaml` | `meta.tarih_senaryolari` (YENİ) |
| `80-model/inputs/vergi.yaml` | `otv_maktu_zaman_serisi.*` (`epistemik_ayrim` YENİ) |
| `80-model/inputs/senaryolar.yaml` | `tarih_senaryolari`, `duyarlilik_eksenleri[OTV]` |
| `80-model/inputs/makro.yaml` | Yİ-ÜFE varsayımı — **hâlâ YOK** (`T-104` ikinci ayağı) |
| `80-model/engine/matrah_sirasi.py` | `:217` ve `vergi_kalemlerini_yukle()` |

### İlişkili kayıtlar

`T-104` (CRITICAL, ANSWERED) — bu ticket onun **birinci ayağıdır**
(*"engine'in bu bloğu fiilen okuduğu doğrulanmamıştır"*). `T-104` kapanışı
artık **T-921'e bağlıdır**.
`T-153` (MEDIUM) — `is None` denetiminin yetersizliği; **T-921 onu kapsar**.
`OQ-002` — `INPUT_RECORDED`, kapanmadı.

---

## CEVAP *(finans-fizibilite — 2026-08-10)*

### Bulgu

**İddianın tamamı doğrulanmıştır.** TUR 2.5 öncesi tarama:

```
$ grep -rn "otv_maktu_zaman_serisi|son_gozlem_gecerlilik_ufku|BASE_DATE|asgari_maktu_tutar" \
       80-model/engine/*.py
  -> 0 eslesme
```

Tarihle ilgili tek satır `matrah_sirasi.py:217`'deki `is None` kontrolüydü ve
`model_hedef_tarihi = 2027-04-01` yazıldığı anda **fiilen susmuştu**.
Başkanın tarif ettiği **arıza zinciri (1→6) kodda birebir mevcuttu.**

### Kabul kriterlerinin karşılanması

| # | İstenen | Nerede | Durum |
|---|---|---|---|
| **1** | `otv_maktu(t)` fonksiyonu; `gozlenen_degerler` içinden `effective_date <= t` olan **en son** kaydı seçer | **`80-model/engine/otv_zaman_serisi.py`** (YENİ, 240 satır) → `otv_maktu()` adım 1–2 | ✅ |
| **2** | **Ufuk denetimi**: `t > son_gozlem_gecerlilik_ufku` **ve** seçili senaryo noktası yoksa → `UNKNOWN` + eksik girdi `otv_maktu_zaman_serisi.gelecek_degerler` | aynı dosya, adım 3 | ✅ |
| **3** | `matrah_sirasi[sira=4].asgari_maktu_tutar` **doğrudan okunmasın**; çelişirse **seri esas** | aynı dosya — `engine_yasak denetimi` bloğu; kısayol yalnız **karşılaştırma** için okunur, hesapta **kullanılmaz** | ✅ |
| **4** | `model_hedef_tarihi_status != FACT` ise çıktı etiketlensin (`T-153` de kapsanır) | aynı dosya + `matrah_sirasi.py` (yeni blok) | ✅ |
| **5** | `EARLY`/`BASE`/`LATE` **ayrı ayrı**; ortalama alınmaz; sınır tarihleri **iki rejim ihtimaliyle** | `calistir_tur25.py` → `tablo_lambda()`; çıktı: `reverse-price-model.md` §8.2 | ✅ |

### Bayrak sözleşmesi

```
otv_maktu(t="2027-04-01", otv_senaryo=None)
    -> status=UNKNOWN, hesaplandi=False
       eksik: "otv_maktu_zaman_serisi.gelecek_degerler = null ... (O-5, T-921)"

otv_maktu(t="2027-04-01", otv_senaryo="UPPER_BOUND_LAMBDA_1")
    -> status=UPPER_BOUND, lambda=1, otv/sise=53,4519
       ZORUNLU etiketler: O-2, O-3, O-5, O-6
       cikti alani adi: cif_try_max_UPPER_BOUND   (cif_try_max DEGIL)

otv_maktu(t=..., otv_senaryo="PROJEKSIYON_ASSUMPTION", lambda=...)
    -> status=PROJEKSIYON_ASSUMPTION  (ASLA FACT)
```

### Fiilî doğrulama

`python3 80-model/engine/hesap.py` (bayraksız, varsayılan yol):

```
EKSIK GIRDILER (21):
  - otv_maktu_zaman_serisi.gelecek_degerler = null. model_hedef_tarihi (2027-04-01)
    > son_gozlem_gecerlilik_ufku (2026-12-31) ve senaryolar.yaml'da SECILMIS bir
    OTV artis ASSUMPTION'i YOK -> engine UNKNOWN doner
    (engine_okuma_kurali adim 3, O-5, T-921).
```

`python3 80-model/engine/test_ters_model.py` → **TV-10 GEÇTİ**:
bayraksız `UNKNOWN`; bayraklı `UPPER_BOUND` + `O-2/O-3/O-5/O-6` etiketleri
+ `cif = 97,6987` (spesifikasyonun beklediği değer).

### Kanıt

| evidence_id | tier | claim | effective_date |
|---|---|---|---|
| — | — | **Kanıt kartı AÇILMADI.** Bu bir dış olgu değil, bir **kod davranışıdır**; `10-evidence/` bu ajan tarafından açılmaz (CLAUDE.md §7) | — |
| `EV-2026-08-09-111` | T2 | Okunan tek ÖTV çapası: 71,2692 TRY/lt | **2026-07-03** |
| `EV-2026-08-09-114` | T1 | ÖTVK md.12/3 — Ocak/Temmuz otomatik güncelleme | — |

### Sonuç

**Beş kabul kriterinin beşi de karşılanmıştır. `P-1` kapısı açılmıştır.**

**2027 ÖTV tutarı hiçbir yerde yazılmamış, tahmin edilmemiş veya
türetilmemiştir.** Kullanılan tek sayı `71,2692 TL/lt` (eff. **2026-07-03**)
**çapasıdır**; çıktılar `cif_try_max_UPPER_BOUND` adıyla ve **O-2/O-3/O-5/O-6
etiketleriyle** raporlanmıştır.

**`T-104`'ün birinci ayağı bu ticket'la kapanmıştır.** İkinci ayak
(`makro.yaml` Yİ-ÜFE `ASSUMPTION`'ı) **hâlâ açıktır**; `λ` ekseni
`senaryolar.yaml → duyarlilik_eksenleri[OTV].talep_edilen_senaryo_noktalari`
üzerinden **PROJEKSİYON (ASSUMPTION)** etiketiyle koşulmuştur ve **hiçbir
noktası "beklenen" olarak sunulmamıştır.**

**`T-153` bu ticket tarafından kapsanmıştır** (kabul kriteri #4).

**İlgili çıktı:** `80-model/outputs/reverse-price-model.md` §1.

---

## KAPANIŞ *(yatirim-komitesi-baskani doldurur)*

### Karar

### Gerekçe

### Kapanış tarihi
