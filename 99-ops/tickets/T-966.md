# TICKET T-966

```yaml
ticket_id:            T-966
opened_by:            yatirim-komitesi-baskani
target_agent:         kanal-marj-uzmani
opened_date:          2026-08-10
claim:                "Kurucu 799 TL'yi PRIMARY_TARGET_SHELF_PRICE olarak belirledi ama (a) bu fiyatin HANGI L8 ALT KATMANINA ait oldugunu (N-3b, T-859) ve (b) ters modelde HANGI KANAL zincirinin (TARGET_CHANNEL, T-871 soru 2) kullanilacagini SOYLEMEDI. SORU: (a) ve (b) TEK BIR cevaptan mi turer, yoksa IKI AYRI secim midir? Eger L8 alt katmani secimi kanal zincirini (m_retail, d, f) tek anlamli belirliyorsa yatirimciya TEK soru sorulur; belirlemiyorsa IKI soru sorulmalidir."
impact:               HIGH
status:               OPEN
resolution_evidence:  -
```

---

## AÇIKLAMA

### Ne soruluyor

Bugün yatırımcı listesinde **birbirine benzeyen ama aynı olmayan** iki
soru vardır:

| # | Soru | Kayıt |
|---|---|---|
| **(a)** | `799 TL` **hangi rafın** fiyatıdır? `L8_CHAIN_RETAIL` / `L8_METRO_CASH_CARRY` / `L8_ONLINE_UZMAN_PERAKENDE` / kanal karması | `N-3b`, `T-859` |
| **(b)** | Ters modelde **hangi kanal zinciri** kullanılacak? `CH_RETAIL` / `CH_TEKEL` / `CH_HORECA` | `T-871` soru 2, `kanal_senaryolari` |

**Sorulan tam soru:** Bunlar **tek bir seçim midir, iki midir?**

1. `L8` alt katmanı seçilirse, o katmanın `m_retail` / `d` / `f` zinciri
   **tek anlamlı** mıdır? (Örn. `L8_METRO_CASH_CARRY` seçilirse kanal
   otomatik olarak cash & carry midir, yoksa aynı rafa **farklı kanal
   yapılarıyla** ulaşılabilir mi?)
2. Tersi doğru mudur — kanal seçilirse `L8` alt katmanı belirlenir mi?
3. **Kanal karması** seçilirse `L8` ne olur? Karma bir `L8` alt katmanı
   **tanımlı mıdır**, yoksa her kanal kendi `L8`'ini mi taşır?

### Neden önemli — model nerede kırılır

`T-871`'in kendi tablosuna göre kanal değişimi tavanı **anlamlı ölçüde**
oynatır: `799 BASE 5.000`'de `CHAIN` **272,83** ↔ `INDEPENDENT/TEKEL`
**303,90** (**+%11,4**). Ve `T-856`'ya göre bu farkın **tamamı** `d`'nin
tekelde `0` alınmasından gelmektedir — yani **bir `UNKNOWN`'dan.**

> **Yatırımcıya iki ayrı soru sormak ile tek soru sormak arasındaki fark,
> yatırımcının vereceği cevabın anlamını değiştirir.** Eğer bunlar tek bir
> seçimse, bugün `MUST DECIDE NOW`'da **bir** kalem vardır; iki seçimse
> **iki** kalem vardır ve ikincisi bugün listede **yoktur.**
>
> Başkan bu bağı **kendi kurmamıştır** — kanal zincirinin tanımı bu ajanın
> alanıdır.

⚠ Ayrıca `T-951` (`d`/`f` mekanizması — fatura indirimi mi ayrı hizmet
faturası mı) bu soruyla **kesişir**: mekanizma kanala göre değişiyorsa
(`EV-2026-08-10-610` snapshot'ındaki iki zıt cümle), tek bir `d`
katsayısı zaten kanal ayrımını gizlemektedir.

### Hangi model girdisini etkiler

| YAML dosyası | Alan |
|--------------|------|
| `80-model/inputs/kanal.yaml` | hangi kanal `base_case`; `m_retail`, `d`, `f` |
| `80-model/inputs/senaryolar.yaml` | `kanal_senaryolari[*].kanal_karmasi` (bugün `null`), `hedef_raf_fiyati_merdiveni.investor_rol_atamasi_tur325.ek_baglayici_kurallar.TR325-4` |
| `80-model/inputs/pazar.yaml` | `l8_chain_retail` (bugün `null`), `K7` katman kuralı |

### İlgili kayıtlar

`T-859` (HIGH) · `T-871` (HIGH) · `T-856` · `T-951` (HIGH) · `T-603` ·
`T-701` · `T-917` · `investor-decisions-required.md` `N-3`

---

## CEVAP *(target_agent doldurur)*

### Bulgu

### Kanıt

| evidence_id | tier | claim | effective_date |
|-------------|------|-------|----------------|
| | | | |

### Sonuç

`status` → `ANSWERED`

---

## KAPANIŞ *(yatirim-komitesi-baskani doldurur)*

### Karar

### Gerekçe

### Kapanış tarihi
